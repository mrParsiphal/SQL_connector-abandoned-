window:
    panel:
     = BoxLayout(size_hint=[1, .15])

    Button:
    (size_hint=[None, None], pos_hint={'top': 1}, size=[50, 20], text="1"))

    Button:
    (size_hint=[None, None], pos_hint={'top': 1},  size=[50, 20], text="2"))

    Button:
    (size_hint=[None, None], pos_hint={'top': 1},  size=[50, 20], text="3"))

    Button:
    (size_hint=[None, None], pos_hint={'top': 1},  size=[50, 20], text="4"))

    Button:
    (size_hint=[None, None], pos_hint={'top': 1}, size=[50, 20], text="5"))

    Button:
    (size_hint=[None, None], pos_hint={'top': 1}, size=[50, 20], text="6"))

    BoxLayout():
        left_panel:
        (size_hint=[.15, 1], orientation = 'vertical')
            Button:
            (text="7", size_hint=[None, None], pos_hint={'top': 1}))
            Button:
            (text="8", size_hint=[None, None], pos_hint={'top': 1}))
            Button:
            (text="9", size_hint=[None, None], pos_hint={'top': 1}))
        FloatLayout():
            Label:
            (text='Привет мир!')